#ifndef VNLC_IDENTIFIER_LIKE_PARSING_CONTEXT_HPP
#define VNLC_IDENTIFIER_LIKE_PARSING_CONTEXT_HPP

#include <string>
#include <vector>

namespace vnlc {
    struct IdentifierLikeParsingContext {
        std::vector<std::string> prefix;
    };
} // namespace vnlc

#endif // VNLC_IDENTIFIER_LIKE_PARSING_CONTEXT_HPP
